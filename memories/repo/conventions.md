- 2026-03-17: Azure deployment is GitHub Actions plus Bicep driven; operators provide subscription ID, resource group, and environment at workflow runtime while Bicep provisions App Service, Key Vault, App Insights, and Azure OpenAI.
- 2026-03-17: Pin GitHub Actions to commit SHAs in workflow files; use actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 instead of tag references.
- 2026-03-19: The active runtime persistence module is `server/databaseStore.js`; it owns schema-version resets and supports multi-qualification specs, shared units, learning outcomes, and assessment criteria.
- 2026-03-20: Runtime job responses are enriched in `server/jobStore.js` with derived structure summary counts and review readiness; the client should read those fields instead of inferring persistence state from hard-coded nodes.
- 2026-03-21: The extraction pipeline now treats the uploaded PDF artifact as the authoritative AI input; `server/extractionService.js` may parse locally only for page-count/source-excerpt metadata, while qualification structure generation must come from `server/aiClient.js` via the Responses API file-input path.
- 2026-03-21: When Azure/OpenAI extraction uses `responses.create()`, connectivity checks must also hit the Responses API; a chat-only health probe can miss stale `FOUNDRY_API_VERSION` or deployment mismatches that only surface on the live extraction path.
- 2026-03-21: The live dev Azure deployment currently resolves to web app `app-qualsdemo-dev-2rbma5` and Azure OpenAI account `aoaiqualsdemodev2rbma5fj` in resource group `rg-QualsDemo`; verify App Service settings directly with `az webapp config appsettings list` when debugging live Foundry issues.
- 2026-03-21: On App Service, in-process extraction jobs must have an outer watchdog and startup reschedule path; otherwise a recycle or unresolved promise can strand jobs in `processing` forever even when direct Azure `responses.create()` calls succeed.
- 2026-03-21: The active extraction path is now Azure AI Document Intelligence `prebuilt-layout` with markdown output -> `server/aiClient.js` authoritative JSON extraction; use live job reprocess on the stored artifact when the original PDF is not available locally.
- 2026-03-21: For App Service Key Vault references, do not attach secret-backed app settings directly in the initial `Microsoft.Web/sites` resource when the same deployment also creates the site identity and Key Vault access policy. Apply them in a separate `Microsoft.Web/sites/config` resource that depends on the access policy and secrets.