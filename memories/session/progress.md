- 2026-03-17: Simplified deploy pipeline to remove manual Foundry inputs; deploy workflow now accepts subscription_id, resource_group, and environment.
- 2026-03-17: infra/main.bicep now provisions Azure OpenAI account and deployment, and README cloud deployment guidance matches the workflow.
- 2026-03-17: Validation completed with clean diagnostics for deploy.yml, ci.yml, main.bicep, and README; npm test passes 16/16 locally.
- 2026-03-16: Added a local .env configured for Azure AI Foundry with placeholder FOUNDRY_API_KEY and FOUNDRY_ENDPOINT values for local development.
- 2026-03-17: Fixed local startup by lazy-loading pdf-parse so DOMMatrix import errors no longer crash the server at boot; npm test now passes and /api/v1/health responds locally.
- 2026-03-17: Added automatic .env loading for local runtime entrypoints; ai:check now reads .env but the current Foundry config still returns 400 API version not supported.
- 2026-03-17: Aligned local Foundry docs and .env templates with the repo's Azure OpenAI deployment shape: endpoint placeholder now uses .openai.azure.com, API version is 2024-10-21, and model is gpt-4o-mini.
- 2026-03-17: deploy.yml now supports either AZURE_CREDENTIALS JSON or split Azure auth values (AZURE_CLIENT_ID, AZURE_CLIENT_SECRET, AZURE_TENANT_ID) for GitHub environment-based deployment.
- 2026-03-17: Pushed commit 912b7fb to origin/main with OIDC-capable deploy workflow, GitHub variable fallback for subscription/resource group, README deployment updates, and centralus environment parameter changes.
- 2026-03-17: GitHub CLI token could push code but could not dispatch the workflow or read environment variables/secrets; Actions APIs returned 403 admin-rights errors, so deployment must be started by a repo admin or through the GitHub web UI.
- 2026-03-18: Pushed commit b2023f3 to origin/main to change the Azure OpenAI deployment default from Standard to GlobalStandard after centralus what-if failed for gpt-4o-mini 2024-07-18 with InvalidResourceProperties.
- 2026-03-18: Pushed commit 61c5246 to origin/main to register required Azure resource providers in deploy.yml and make the Key Vault access-policy child resource explicit after run 23224067046 failed on Microsoft.Web registration and accessPolicies payload handling.
- 2026-03-18: Run 23224145389 is blocked by Azure RBAC outside the repo: the OIDC service principal lacks Microsoft.Web/register/action at the subscription scope, so provider registration must be done once by an identity with subscription-level registration rights.
- 2026-03-18: Run 23224294377 showed the workflow fix still needed Azure CLI compatibility work: the hosted runner CLI does not support `az provider wait`, so deploy.yml now polls `az provider show` until registrationState becomes Registered.
- 2026-03-18: Run 23224337788 showed the remaining Key Vault issue was the vault create payload, not the child update alone; infra/main.bicep now sets `properties.accessPolicies: []` on the vault and keeps the child access-policy resource in the documented `parent + name: add` form.
- 2026-03-18: Run 23224425956 got through infrastructure deployment and failed only on the post-deploy Key Vault secret write; the fix moves `foundry-api-key` creation into Bicep as a `Microsoft.KeyVault/vaults/secrets` resource sourced from the Azure OpenAI account key and removes the workflow secret-seeding step.
- 2026-03-19: Qualification extraction schema now supports multiple qualifications per document plus learning outcomes and assessment criteria; runtime persistence moved to `server/databaseStore.js` with schema-version reset so legacy deployed sample data is cleared on startup.
- 2026-03-19: UI reset/startup copy no longer refers to demo seed data, and `/api/v1/reset` now returns the app to an empty workspace because `server/seed-data.json` remains empty.
- 2026-03-19: Updated `docs/artifacts/prd/PRD-1.md` to align product requirements with the revised `prompts/qualification-extractor.md`, adding explicit requirements for multi-qualification documents, shared units, learning outcomes, assessment criteria, command-verb capture, and pre-approval validation of GLH, credits, mandatory units, and optional-group rules.
- 2026-03-19: Updated `docs/artifacts/specs/SPEC-1.md` to align the technical design with the revised PRD, including shared-unit semantics, learning outcomes, assessment criteria, expanded submission payloads, validation rules, and evaluation coverage.
- 2026-03-19: Updated `docs/ux/UX-1.md` so the review UX explicitly covers shared units, learning outcomes, assessment criteria, validation summaries, and approval override rationale.
- 2026-03-19: Added `docs/execution/plans/BACKLOG-1-prd-refresh.md` as a local issue-ready backlog artifact because the workspace does not expose a live issue tracker.
- 2026-03-19: Updated `docs/ux/prototypes/index.html` and `docs/ux/prototypes/review.html` so the HTML prototypes now reflect multi-qualification intake, shared-unit indicators, learning-outcome and assessment-criteria review, validation summary navigation, and approval override behavior.
- 2026-03-19: Updated the live client in `app/index.html`, `app/assets/app.js`, and `app/assets/styles.css` to match the refreshed review workflow, including qualification tabs, validation summary rail, shared-unit indicators, nested outcome and criterion review, and approval override controls.
- 2026-03-19: Extended `server/jobStore.js`, `server.js`, and `tests/jobStore.test.js` so jobs now expose derived validation summaries, support approval override persistence via extraction metadata, update shared units across linked qualifications, and keep approval gated by real review readiness. 
- 2026-03-19: Added `tests/server.integration.test.js` to exercise the real HTTP server, verify the shipped review workspace landmarks, and validate the approval-override flow end to end against the SQLite-backed runtime.
- 2026-03-19: Added Playwright browser coverage in `tests/e2e/review-workspace.spec.js`, fixed the shared-unit selector to scope clicks to `#hierarchyTree .tree-card`, and verified `npm run test:all` passes with 21 Node tests plus 1 browser e2e test.
- 2026-03-19: Added `.vscode/` and `test-results/` to `.gitignore` so local editor settings and Playwright run artifacts stay out of the push; re-ran `npm run test:all` successfully before committing.
- 2026-03-20: Simplified the review flow to structure-first persistence: review jobs now become approvable once qualification structures exist, the UI shows discovered qualification counts instead of blocker and warning summaries, and hierarchy branches are expandable and collapsible for navigation across multi-qualification specifications.
- 2026-03-20: Updated ADR-1, SPEC-1, and BACKLOG-1 so the docs now describe structure-first review, discovered-qualification summaries, collapsible hierarchy groups, and persistence without blocker or override gating.
- 2026-03-20: Rewrote `prompts/qualification-extractor.md` into a structured system prompt aligned with the JSON schema and current product contract, including multi-qualification extraction, shared-unit identity reuse, nested learning outcomes and assessment criteria, command-verb capture, uncertainty markers, and structure-first `reviewReady` guidance; `npm test` passes 22/22 after the change.
- 2026-03-20: Investigated GitHub Actions run 23365822082; `Deploy Azure Stack` failed in the `infra` job during `Deploy infrastructure` with Azure `RequestConflict` because the OpenAI account resource was still in a non-terminal provisioning state. Added a targeted retry loop in `.github/workflows/deploy.yml` for that transient condition and switched `infra/main.bicep` from `listKeys(...)` to `openAiAccount.listKeys()` to match Bicep guidance.
- 2026-03-20: Added `docs/artifacts/prd/PRD-2.md` and `docs/execution/plans/BACKLOG-2-authoritative-contract-migration.md` to define the product migration from the new authoritative `Qualifications` extractor contract to a two-contract architecture with an explicit normalization boundary that preserves the current review workspace in phase 1.
- 2026-03-20: Added `docs/execution/plans/ISSUES-2-authoritative-contract-migration.md` with paste-ready Epic, Feature, and Story issue bodies for the authoritative-contract migration so the backlog can move directly into tracker execution.
- 2026-03-21: Added `docs/execution/plans/FILEMAP-2-authoritative-contract-migration.md` and aligned `docs/artifacts/specs/SPEC-1.md` plus `docs/artifacts/adr/ADR-1.md` so the technical and architectural docs now describe the two-contract migration: authoritative AI schema externally, normalized internal review graph internally.
- 2026-03-21: Implemented the phase-1 compatibility boundary with `templates/qualification-extraction-authoritative-schema.json`, `server/aiDraftNormalizer.js`, an updated `server/aiClient.js` that validates authoritative payloads and normalizes them before merge, extraction metadata merging in `server/extractionService.js`, and new regression tests in `tests/aiClient.test.js` plus `tests/aiDraftNormalizer.test.js`.
- 2026-03-21: Validated the authoritative-contract migration with clean editor diagnostics and a passing `npm test` run: 24/24 tests passed, including the new AI client normalization and normalizer regression coverage.
- 2026-03-21: Updated the app's tracked Azure Foundry defaults from `gpt-4o-mini` to `gpt-5` by changing `.env.example`, `app/README.md`, `README.md`, and `infra/main.bicep` to use model `gpt-5` with Azure OpenAI model version `2025-08-07`; `npm test` still passes 24/24.
- 2026-03-21: Refactored runtime extraction to send the uploaded PDF directly to the AI through the Responses API `input_file` path, updated the extractor prompt/schema to make the attached PDF the only evidence source, removed heuristic runtime draft generation from `server/extractionService.js`, and updated AI/normalizer/job tests; final validation passed with `npm test` at 23/23.
- 2026-03-21: Updated `docs/artifacts/adr/ADR-1.md`, `docs/artifacts/specs/SPEC-1.md`, `README.md`, and `app/README.md` so the documented architecture now matches the direct-PDF AI extraction runtime, the authoritative-to-internal normalization boundary, and the removal of heuristic extraction from the active path.
- 2026-03-21: Updated `app/assets/app.js` so failed AI extraction attempts surface `extractionMeta.aiError` in the dashboard, review summary, empty hierarchy state, and approval panel; editor diagnostics were clean and `npm test` remained green at 23/23 before push.
- 2026-03-21: Updated Azure Foundry defaults to `FOUNDRY_API_VERSION=2025-03-01-preview` in `infra/main.bicep`, `.env.example`, `README.md`, and `app/README.md`, and changed `runAiConnectivityCheck()` in `server/aiClient.js` to use `responses.create()` so the health check validates the same Azure route as live PDF extraction; `npm test` passes 23/23.
- 2026-03-21: Verified the live dev Azure deployment in subscription `Visual Studio Enterprise` as web app `app-qualsdemo-dev-2rbma5` in `rg-QualsDemo`; App Service had stale `FOUNDRY_API_VERSION=2024-10-21` while Azure OpenAI deployment `gpt-5` existed, so the live fix was to set `FOUNDRY_API_VERSION=2025-03-01-preview`, zip-deploy the current workspace, and confirm `/api/v1/health` and `/api/v1/ai-status` both respond successfully.
- 2026-03-21: Diagnosed the post-fix Azure 400 by reproducing calls directly against `aoaiqualsdemodev2rbma5fj`: `AzureOpenAI` with `/openai/v1/` plus `apiVersion` returns `400 API version not supported`, while `AzureOpenAI` with `endpoint` plus `apiVersion=2025-03-01-preview` works; `gpt-5` also rejects `temperature` and requires `max_output_tokens >= 16`. Updated `server/aiClient.js` accordingly, reran `npm test` with 26/26 passing, redeployed, restarted the App Service, and verified the live upload path no longer returns the Azure 400.
- 2026-03-21: Diagnosed the subsequent `Request timed out` failures as the app's own `openai` client timeout: `server/aiClient.js` was capped at 15000ms for all providers. Added `QUAL_AI_TIMEOUT_MS` with a 120000ms default, documented it in `.env.example`, `README.md`, and `app/README.md`, reran tests successfully, redeployed Azure with `QUAL_AI_TIMEOUT_MS=120000`, and verified a live smoke upload remains in `processing` beyond 30 seconds without the old timeout error.
- 2026-03-21: Debugged the remaining stuck-processing behavior: direct Azure extraction for the smoke PDF returns a normal model-level error in about 35 seconds, so the hang is not the Azure route itself. Added an outer extraction watchdog in `server/extractionService.js`, persisted worker-start metadata in `server/jobStore.js`, and rescheduled leftover `processing` jobs on startup in `server.js`; added regression tests for watchdog timeout and startup recovery, and `npm test` passed after the change.
- 2026-03-21: Switched runtime extraction from raw PDF-to-LLM to Azure AI Document Intelligence `prebuilt-layout` markdown -> LLM. Added `server/documentIntelligenceClient.js`, updated `server/extractionService.js`, `server/aiClient.js`, prompt/env/docs/UI, expanded tests, and verified `npm test` passed locally.
- 2026-03-21: Deployed the updated Bicep to `rg-QualsDemo`, which created Document Intelligence account `diqualsdemodev2rbma5fjnm`, updated App Service settings, zip-deployed the app, and verified `/api/v1/ai-status` now reports both Foundry and Document Intelligence as configured.
- 2026-03-21: Live attached-PDF validation used stored artifact job `job-1774106082815` because the original PDF was not present locally. Initial reprocess failed with Document Intelligence 401 despite config showing present, but a direct Azure repro proved the request shape was correct; overriding `DOCUMENT_INTELLIGENCE_API_KEY` on the web app with the verified live key unblocked the run. Final reprocess completed to `review` with `qualificationCode=600/4342/8`, `reviewReady=true`, `authoritativeQualificationCount=3`, and Document Intelligence metadata including `pageCount=288`, `tableCount=179`, and `sectionCount=606`.
- 2026-03-21: Added runtime detection for unresolved App Service Key Vault reference strings in `server/aiClient.js` and `server/documentIntelligenceClient.js`, plus regression tests. Refactored `infra/main.bicep` so secret-backed app settings are applied through `Microsoft.Web/sites/config` after the Key Vault access policy exists, then redeployed infra and app code.
- 2026-03-21: Post-redeploy `/api/v1/ai-status` returned `configured: true` for both Foundry and Document Intelligence with no unresolved-reference issues, which indicates the live Key Vault-backed settings are now resolving. A fresh reprocess of `job-1774106082815` was accepted (`202`) and started a new worker attempt after app restart; the last direct poll during this session still showed the large document in `processing`, with no renewed 401 or configuration error yet surfaced.
- 2026-03-21: Deployed the current frontend workspace to live App Service `app-qualsdemo-dev-2rbma5` with the established zip-deploy flow. Live verification confirmed the Pearson SVG header is served, the review layout uses the wider PDF column, the PDF frame has the slim black margin, and the review-page JS no longer renders the focus overlay, `Review state` block, or `Ready to persist` summary card. `npm test` reported 34 passing tests and 1 failing integration assertion that still expects the old shell text `Qualification review workspace`.
- 2026-03-21: Fixed the broken Pearson logo on the live site by inlining the supplied SVG directly into `app/index.html` and redeploying. Local tests are now green at 36/36 after also adding explicit SVG content-type coverage and updating the shell integration assertion. Live HTML verification confirms the page no longer references `./assets/pearson-logo.svg` and now contains the inline brand SVG markup.
- 2026-03-22: Refreshed `docs/artifacts/adr/ADR-1.md` and `docs/artifacts/specs/SPEC-1.md` so the architecture documentation now matches the current runtime: uploaded PDFs are stored on the server filesystem, extraction runs through Azure AI Document Intelligence markdown before the Responses API call, `server/databaseStore.js` is the active persistence module, and Application Insights export remains a future observability enhancement rather than a current capability.
- 2026-03-22: Followed through on the remaining doc drift in `README.md`, `app/README.md`, and `docs/ux/UX-1.md`: removed stale direct-PDF-to-model wording, replaced old `ready-to-persist` flow language with the current structure-first approval posture, and marked the UX artifact as accepted.
- 2026-03-22: Completed a repo-wide active-documentation sweep. Updated `docs/artifacts/prd/PRD-1.md` to use repo-relative links for `QualStructure.md` and the extractor prompt, corrected the remaining stale ADR/spec/UX wording around direct PDF input and persistence readiness, and left only historical plans, reviews, and session notes as records of superseded runtime states.